<Page
    x:Class="RecipeBook.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecipeBook"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel>
        <ListView Name="recipesList" ItemsSource="{x:Bind recipes}" SelectionChanged="RecipesList_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:RecipeDispHome">
                    <StackPanel Orientation="Horizontal" Height="160" Width="1000">
                        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
                            <TextBlock Text="{x:Bind Name}" VerticalAlignment="Center" Name="ProductName" FontSize="42" FontWeight="Bold" Margin="20 0 0 3" Width="300"/>
                            <TextBlock Text="{x:Bind CategoryName}" VerticalAlignment="Center" Name="ProductQuantity" Margin="0" Opacity="0.8" FontSize="32" Width="240"/>
                        </StackPanel>
                        <Image Name="RecipeImage" Source="{x:Bind ImgAddress}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</Page>
